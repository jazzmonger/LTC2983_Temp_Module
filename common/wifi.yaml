---
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  # UI + UART to IO board work without WiFi; enable after boot screen (10.1 Display.yaml).
  enable_on_boot: false
  fast_connect: true
  ap:
    password: !secret wifi_password

captive_portal:

web_server:
  port: 80
  version: 3
